Challenge de nodejs

instalacion:
  -configurar .env: 
    PORT=3000
    MONGO_URI='mongodb://mongodb:27017/project_management'
    JWT_SECRET='prueba'

  -correr:
    npm run build
  -correr:
    docker compose up --build -d